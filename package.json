{
  "name": "react-test",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://o-mar.github.io/react-test-app/",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "npm run build&&gh-pages -d build",
    "eject": "react-scripts eject"
  }
}
